<% if flash[:notice] %>
<p><%= flash[:notice] %></p>
<% end %>
    <div class="body">
      <div class="container">

        <p id="notice"></p>


        <div class="row">
  
  <div class="col-xs-3">
    <h2>User info</h2>
     <%= attachment_image_tag @user, :profile_image, size: '50x50', format: 'jpeg', fallback: "noimage.jpg" %><table class="table">
      <tbody><tr></tr> 
      <tr>
        <th>name</th>
        <th><%= @user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th><%= @user.introduction %></th>
        
      </tr>c
</tbody></table>


<div class="row">
  <%= link_to "", edit_user_path(@user.id), class: "glyphicon glyphicon-wrench col-xs-12 btn btn-default"%>
</div>

    <h2>New book</h2>
   <%= form_for(@book) do |f| %>
  <div class="field row">
    <h4>Title</h4>
  <%= f.text_field :title %>
  </div>

  <div class="field row">
   <h4>Body</h4>
  <%= f.text_area :body %>

  </div>

  <div class="actions row">
  <%= f.submit 'Create Book' %>
  </div>
<% end %>
  </div>
<div class="col-xs-9">
  <table class="table table-hover table-inverse">
    <thead>
    <tr>
      <th>Book detail</th>
      <th colspan="2"></th>
      <th colspan="3"></th>
    </tr>

    <tr>
      <td><%= link_to user_path(@user) do %><%= attachment_image_tag @user, :profile_image, size: '50x50', format: 'jpeg', fallback: "noimage.jpg" %>
      <%= @user.name %>
      </td>
      <% end %>
      <td><%= @book.title %></td>
      <td><%= @book.body %></td>
      <% if @user.id == current_user.id %>
      <td><%= link_to "Edit", edit_book_path(@book.id) %></td>
      <td><%=link_to "Destroy", book_path(@book), method: :delete %></td>
      <% else %>
      <td></td>
      <td></td>
      <% end %>
    </tr>
  </thead>
</table>
</div>   

    <footer>
  <div class="container">
    <div class="row">
      <div class="col-xs-3 col-xs-offset-9">
        <p>CopyRight Infratop.inc</p>
      </div>
    </div>
  </div>
</footer>


