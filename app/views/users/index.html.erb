    <div class="body">
      <div class="container">

        <p id="notice"></p>


        <div class="row">
  
  <div class="col-xs-3">
    <h2>User info</h2>
    <%= attachment_image_tag current_user, :profile_image, size: '50x50', format: 'jpeg', fallback: "noimage.jpg" %>
    <table class="table">
      <tbody><tr></tr> 
      <tr>
        <th>name</th>
        <th><%= @user.name %></th>
      </tr>
      <tr>
        <th><%= @user.introduction %></th>
        <th></th>
      </tr> 
</tbody></table>
<div class="row">
  <%= link_to "", edit_user_path(current_user.id), class: "glyphicon glyphicon-wrench col-xs-12 btn btn-default"%>
</div>

    <h2>New book</h2>
   <%= form_for(@book) do |f| %>
  <div class="field row">
    <h4>Title</h4>
  <%= f.text_field :title %>
  </div>

  <div class="field row">
   <h4>Body</h4>
  <%= f.text_area :body %>

  </div>

  <div class="actions row">
  <%= f.submit 'Create Book',class:"btn btn-primary" %>
  </div>
<% end %>
</div>
<div class="col-xs-9">
   <table class="table table-hover table-inverse">
    <thead>
     <tr> 
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
    <% @users.each do |user| %>
    <tr>
      <td><%= attachment_image_tag user, :profile_image, size: '50x50', format: 'jpeg', fallback: "noimage.jpg" %></td>
      <td><%= user.name %></td>
      <td><%=link_to "Show", user_path(user.id) %></td>
    </tr>
    <% end %>
  </thead>
</table>
</div>